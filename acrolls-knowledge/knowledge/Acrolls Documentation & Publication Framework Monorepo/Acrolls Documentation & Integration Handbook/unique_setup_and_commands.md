Integration walkthrough uses the published `acrolls` npm package and its CLI: `pnpm add acrolls@latest` followed by `pnpm exec acrolls onboard --docs-dir docs --base-href /docs`; verification command is `pnpm exec acrolls --version`.